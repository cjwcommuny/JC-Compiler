namespace Fib {

    def void main(String[] args) {
        print(fib(10));
    }

    def int fib(int n) {
        if (n == 1 || n == 2) {
            return 1;
        }
        return fib(n - 1) + fib(n - 2);
    }
}

/*
output:
55%
*/