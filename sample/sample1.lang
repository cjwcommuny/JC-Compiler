namespace MyNamespace {    
    def int sum(int limit) {
        int sum = 0;
        for (int i = 0; i < limit; i = i + 1) {
            sum = sum + i;
        }
        return sum;
    }

    def void main() {
        sum(10);
    }
}
