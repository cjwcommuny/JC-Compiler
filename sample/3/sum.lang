namespace Sum {
    def int sum(int limit) {
        int sum = 0;
        for (int i = 0; i < limit; i = i + 1) {
            sum = sum + i;
        }
        return sum;
    }

    def void main() {
        int s = sum(10);
        println("", s);
    }
}
