package ast.node.definition;

import ast.node.Node;
import type.typetype.Type;

public class StructureFieldDefinitionNode extends DefinitionNode {
    public StructureFieldDefinitionNode(Type type) {
        super(type);
    }
}
